server:
  port: 5557

spring:
  cloud:
    config:
      uri: http://127.0.0.1:5556  # 服务启动后，去找5555服务
      name: didispace  # 从github上读取的资源名称，不要加 .yml后缀
      label: config-test # master分支
      profile: dev  # dev版本